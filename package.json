{
  "name": "nodejs-edit-excels",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prebuild": "rimraf build",
    "build": "pnpm prebuild && tsc -w",
    "precreateFolders": "rimraf generated_folders && mkdirp generated_folders",
    "createFolders": "pnpm precreateFolders && cd build && node createFolders.js",
    "start": "cd build && node app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "exceljs": "^4.4.0",
    "mkdirp": "^3.0.1",
    "rimraf": "^5.0.5"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3"
  }
}
